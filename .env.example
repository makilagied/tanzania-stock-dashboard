# Jolene chart assistant (OpenAI / Anthropic) — at least one key:
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Encrypted IndexedDB memory for Jolene (32 bytes). Generate with: openssl rand -base64 32
# JOLENE_MEMORY_MASTER_KEY=
